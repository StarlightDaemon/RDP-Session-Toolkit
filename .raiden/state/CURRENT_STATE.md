# Current State

## Repo Purpose

Companion repo to [Hide RDP Connection Bar](https://github.com/StarlightDaemon/Hide-RDP-Connection-Bar),
housing the more complex, personal-use-primarily taskbar-integration work that does not
belong in the simple-UI-mod trust boundary of that repo. See root `README.md` for the
full architecture summary.

## Status

Repository freshly bootstrapped on 2026-08-20. Nothing is built yet.

- `taskbar-integration/` — placeholder only; two Windhawk mods planned (client-side
  signal-relay extension, remote-side explorer.exe taskbar widget). Neither exists.
- `dvc-plugin/` — placeholder only; standalone DVC-relay companion executable planned.
  Not started.

## Active Work

None. This session's only activity was the RAIDEN Instance install and initial
directory scaffold.

## Notes

- No GitHub remote configured; this repo is intentionally local-only for now.
